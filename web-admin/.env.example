# Smart Home Platform - Web Admin Environment Variables

# Backend REST API base URL
# Leave as /api when using Vite dev proxy, or provide full URL (e.g. http://localhost:3000/api)
VITE_API_URL=/api

# Backend WebSocket / Socket.IO URL
VITE_WS_URL=http://localhost:3000
